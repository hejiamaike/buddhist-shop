# 梵音阁 - 代码清单

> 完整代码文件列表及关键内容摘要

---

## 一、前台页面 (src/app/)

### 1.1 根布局

**文件：** `src/app/layout.tsx`

```typescript
import type { Metadata } from 'next'
import { LanguageProvider } from '@/contexts/LanguageContext'
import './globals.css'

export const metadata: Metadata = {
  title: '梵音阁 | 佛教文化珍品',
  description: '精选佛教文化珍品，传承千年智慧',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh">
      <body>
        <LanguageProvider>
          {children}
        </LanguageProvider>
      </body>
    </html>
  )
}
```

---

### 1.2 首页

**文件：** `src/app/page.tsx`

```typescript
import { redirect } from 'next/navigation';

export default function Home() {
  redirect('/intro');
}
```

---

### 1.3 心经介绍页

**文件：** `src/app/intro/page.tsx`

- 核心经文展示（观自在菩萨...）
- 简化版静态页面
- 进入按钮跳转到 /products
- 状态：`show` 控制淡入动画
- 行数：80行

---

### 1.4 产品列表页（核心购物页）

**文件：** `src/app/products/page.tsx`

- **行数：544行**
- 数据：6个硬编码产品（PRODUCTS_ZH / PRODUCTS_EN）
- 功能：
  - 搜索框 + 热门搜索建议
  - 分类下拉菜单（馆藏）
  - 胶囊筛选器（材质/用途/价格）
  - 禅意空间场景展示
  - 产品网格
  - 禅意百科文章
  - Footer（品牌/链接/承诺条）
- 样式：深色主题，金色点缀

---

### 1.5 产品详情页

**文件：** `src/app/product/[slug]/page.tsx`

- **行数：673行**
- 6个产品详细数据（包含工艺、故事、保养说明）
- 功能：
  - 多图展示 + 图片切换
  - 中英双语描述
  - 工艺流程展示
  - 规格参数表格
  - 保养须知
  - 加入购物车
- 样式：深色主题，产品叙事设计

---

### 1.6 分类页

**文件：** `src/app/categories/page.tsx`

- 5个分类卡片（佛像/佛珠/香烛/书籍/工艺品）
- 静态数据
- 行数：63行

---

### 1.7 购物车

**文件：** `src/app/cart/page.tsx`

- **行数：125行**
- localStorage 存储
- 功能：
  - 数量增减
  - 删除商品
  - 合计计算
  - 去结算按钮

---

### 1.8 结账页

**文件：** `src/app/checkout/page.tsx`

- **行数：172行**
- ⚠️ 演示模式
- 支付方式选择（支付宝/微信/Stripe/加密货币）
- 订单摘要
- 提示：需配置真实支付

---

### 1.9 订单页

**文件：** `src/app/order/page.tsx`

- **行数：80行**
- localStorage 存储
- 订单状态展示

---

### 1.10 订单成功页

**文件：** `src/app/order/success/page.tsx`

- 21行
- 成功提示 + 导航按钮

---

### 1.11 登录页

**文件：** `src/app/login/page.tsx`

- **行数：144行**
- ⚠️ 模拟登录
- 功能：
  - Google OAuth 按钮（模拟）
  - Apple OAuth 按钮（模拟）
  - 微信登录按钮（模拟）
  - 邮箱+密码登录（模拟）
- 登录成功写入 localStorage

---

### 1.12 注册页

**文件：** `src/app/register/page.tsx`

- **行数：92行**
- ⚠️ 模拟注册
- 直接写入 localStorage

---

### 1.13 个人中心

**文件：** `src/app/profile/page.tsx`

- **行数：69行**
- 检查 localStorage user
- 显示用户信息/订单/购物车入口

---

## 二、后台页面 (src/app/admin/)

### 2.1 后台首页

**文件：** `src/app/admin/page.tsx`

- **行数：67行**
- 5个功能卡片导航

---

### 2.2 管理员登录

**文件：** `src/app/admin/login/page.tsx`

- **行数：76行**
- 密码验证：`buddhist2026`
- 写入 localStorage admin_session

---

### 2.3 商品管理

**文件：** `src/app/admin/products/page.tsx`

- **行数：362行**
- Supabase 数据操作
- 功能：
  - 商品列表表格
  - 添加/编辑弹窗
  - 删除确认
  - 状态切换

---

### 2.4 订单管理

**文件：** `src/app/admin/orders/page.tsx`

- **行数：229行**
- 功能：
  - 订单列表
  - 状态更新（待付款→已付款→处理中→已发货→已送达）
  - 订单详情弹窗

---

### 2.5 分类管理

**文件：** `src/app/admin/categories/page.tsx`

- **行数：250行**
- 卡片式布局
- 双语支持

---

### 2.6 用户管理

**文件：** `src/app/admin/users/page.tsx`

- **行数：139行**
- 邮箱搜索

---

### 2.7 数据统计

**文件：** `src/app/admin/stats/page.tsx`

- **行数：219行**
- 核心指标卡片
- 最近订单列表
- 热销商品排行

---

## 三、组件 (src/components/)

### 3.1 Header.tsx

```typescript
'use client'
import Link from 'next/link'
import { useLanguage } from '@/contexts/LanguageContext'

export function Header() {
  const { t, language, setLanguage } = useLanguage()
  // 简洁版导航：首页/全部商品/分类 + 语言切换 + 购物车/账户
}
```

### 3.2 Footer.tsx

- **状态：** 未使用（各页面内联 footer）

### 3.3 LanguageSwitcher.tsx

- **状态：** 未使用（在 products/page.tsx 内联）

### 3.4 ClientLayout.tsx

- **状态：** 未使用

### 3.5 AdminProtection.tsx

- **状态：** 未使用

### 3.6 enhanced/ 目录

以下组件已创建但未使用：
- SearchBar.tsx
- EnhancedProductCard.tsx
- ProductFilters.tsx
- CultureContent.tsx
- TrustSection.tsx
- FloatingCart.tsx

---

## 四、上下文 (src/contexts/)

### LanguageContext.tsx

```typescript
'use client'
import { createContext, useContext, useState, useEffect, ReactNode } from 'react'

type Language = 'zh' | 'en'

interface LanguageContextType {
  language: Language
  setLanguage: (lang: Language) => void
  t: (zh: string, en: string) => string
}

export function LanguageProvider({ children }: { children: ReactNode }) {
  const [language, setLanguage] = useState<Language>('zh')

  useEffect(() => {
    const saved = localStorage.getItem('language') as Language
    if (saved) setLanguage(saved)
  }, [])

  const handleSetLanguage = (lang: Language) => {
    setLanguage(lang)
    localStorage.setItem('language', lang)
  }

  const t = (zh: string, en: string) => language === 'zh' ? zh : en

  return (
    <LanguageContext.Provider value={{ language, setLanguage: handleSetLanguage, t }}>
      {children}
    </LanguageContext.Provider>
  )
}

export function useLanguage() {
  const context = useContext(LanguageContext)
  if (!context) throw new Error('useLanguage must be used within LanguageProvider')
  return context
}
```

---

## 五、API (src/app/api/)

### 5.1 认证 API

**文件：** `src/app/api/auth/[...nextauth]/route.ts`

```typescript
import NextAuth from 'next-auth'
import CredentialsProvider from 'next-auth/providers/credentials'
import { createClient } from '@supabase/supabase-js'

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
)

const handler = NextAuth({
  providers: [
    CredentialsProvider({
      name: 'Credentials',
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Password", type: "password" }
      },
      async authorize(credentials) {
        const { email, password } = credentials as { email: string; password: string }
        // 查询 Supabase users 表
        // ⚠️ 密码明文比对（生产需 bcrypt）
      }
    })
  ],
  pages: { signIn: '/login' },
  session: { strategy: 'jwt' },
})

export { handler as GET, handler as POST }
```

### 5.2 测试 API

**文件：** `src/app/api/test/route.ts`

---

## 六、样式 (src/app/globals.css)

- **行数：** 约 2400 行
- 包含：
  - CSS 变量（佛教配色）
  - 字体引入（Noto Serif SC / Ma Shan Zheng / Playfair Display）
  - 滚动条样式
  - 按钮样式
  - 卡片样式
  - 搜索框样式
  - 筛选器样式
  - 产品网格
  - 页脚样式
  - 响应式设计
  - 动画效果

---

## 七、TypeScript 类型

**文件：** `src/types/next-auth.d.ts`

```typescript
import NextAuth from "next-auth"

declare module "next-auth" {
  interface Session {
    user: {
      id: string
      name?: string | null
      email?: string | null
      image?: string | null
    }
  }
}
```

---

## 八、配置文件

### package.json

```json
{
  "name": "buddhist-shop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^16.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@supabase/supabase-js": "^2.47.0",
    "next-auth": "^4.24.10"
  }
}
```

### .env.local 示例

```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxx
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key
ADMIN_PASSWORD=buddhist2026
```

---

## 九、代码统计

| 类别 | 文件数 | 备注 |
|------|--------|------|
| 页面文件 | 18 | 前台13 + 后台5 |
| 组件文件 | 11 | 其中6个未使用 |
| API 路由 | 2 | 认证 + 测试 |
| 上下文 | 1 | 语言切换 |
| 类型定义 | 1 | NextAuth |
| 样式文件 | 1 | globals.css |

**总行数估算：** 约 6000 行

---

*清单结束*
