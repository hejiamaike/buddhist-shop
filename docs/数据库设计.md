# 梵音阁 - 数据库设计文档

> Supabase 数据库表结构设计

---

## 1. 数据库概览

- **数据库类型：** PostgreSQL (via Supabase)
- **托管平台：** Supabase
- **当前状态：** 表结构已设计，部分功能使用 localStorage 模拟

---

## 2. 数据表结构

### 2.1 users（用户表）

> 注意：实际使用 Supabase Auth，建议通过 auth.users 管理

```sql
-- 建议通过 Supabase Auth 控制用户，无需手动创建此表
-- 如需扩展用户资料，使用 profiles 表
```

### 2.2 profiles（用户资料表）

```sql
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email TEXT NOT NULL,
  full_name TEXT,
  phone TEXT,
  avatar_url TEXT,
  language TEXT DEFAULT 'zh' CHECK (language IN ('zh', 'en')),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- RLS 策略
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

-- 用户只能查看自己的资料
CREATE POLICY "Users can view own profile" ON profiles
  FOR SELECT USING (auth.uid() = id);

-- 用户可以更新自己的资料
CREATE POLICY "Users can update own profile" ON profiles
  FOR UPDATE USING (auth.uid() = id);
```

### 2.3 categories（分类表）

```sql
CREATE TABLE categories (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  name_en TEXT,
  slug TEXT UNIQUE NOT NULL,
  description TEXT,
  image_url TEXT,
  sort_order INT DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 公开读取
CREATE POLICY "Public read categories" ON categories
  FOR SELECT USING (true);

-- 管理员可增删改
CREATE POLICY "Admin manage categories" ON categories
  FOR ALL USING (
    EXISTS (SELECT 1 FROM admin_users WHERE user_id = auth.uid())
  );
```

**示例数据：**

| name | name_en | slug |
|------|---------|------|
| 佛像 | Buddhas | buddha |
| 手串 | Malas | beads |
| 香炉 | Censers | incense |
| 经书 | Sutras | books |
| 法器 | Rituals | practice |

### 2.4 products（产品表）

```sql
CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  name_en TEXT,
  slug TEXT UNIQUE NOT NULL,
  description TEXT,
  description_long TEXT,
  description_long_en TEXT,
  price NUMERIC(10,2) NOT NULL,
  original_price NUMERIC(10,2),
  stock INT DEFAULT 0,
  images JSONB DEFAULT '[]',
  category_id UUID REFERENCES categories(id),
  material TEXT,
  use_case TEXT,
  tags JSONB DEFAULT '[]',
  status TEXT DEFAULT 'active' CHECK (status IN ('active', 'inactive', 'draft')),
  featured BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 公开读取
CREATE POLICY "Public read products" ON products
  FOR SELECT USING (status = 'active');

-- 管理员可增删改
CREATE POLICY "Admin manage products" ON products
  FOR ALL USING (
    EXISTS (SELECT 1 FROM admin_users WHERE user_id = auth.uid())
  );
```

**示例数据：**

| name | name_en | price | category |
|------|---------|-------|----------|
| 小叶紫檀手串 | Rosewood Mala | 899 | beads |
| 铜镀金释迦牟尼佛像 | Gilded Sakyamuni Buddha | 3680 | buddha |
| 天然沉香线香 | Agarwood Incense | 168 | incense |
| 莲花纹铜香炉 | Lotus Censers | 458 | incense |
| 心经书法卷轴 | Heart Sutra Scroll | 1280 | books |
| 如法棋 | Rufa Oracle Chess | 2520 | practice |

### 2.5 orders（订单表）

```sql
CREATE TABLE orders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  order_number TEXT UNIQUE NOT NULL,
  user_id UUID REFERENCES profiles(id),
  status TEXT DEFAULT 'pending' CHECK (
    status IN ('pending', 'paid', 'processing', 'shipped', 'delivered', 'cancelled')
  ),
  total_amount NUMERIC(10,2) NOT NULL,
  currency TEXT DEFAULT 'CNY',
  payment_method TEXT,
  shipping_name TEXT NOT NULL,
  shipping_phone TEXT NOT NULL,
  shipping_address TEXT NOT NULL,
  note TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 用户只能查看自己的订单
CREATE POLICY "Users view own orders" ON orders
  FOR SELECT USING (auth.uid() = user_id);

-- 管理员可查看所有订单
CREATE POLICY "Admin view all orders" ON orders
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM admin_users WHERE user_id = auth.uid())
  );

-- 管理员可更新订单
CREATE POLICY "Admin update orders" ON orders
  FOR UPDATE USING (
    EXISTS (SELECT 1 FROM admin_users WHERE user_id = auth.uid())
  );
```

**订单状态流转：**

```
pending → paid → processing → shipped → delivered
              ↓
           cancelled
```

### 2.6 order_items（订单明细表）

```sql
CREATE TABLE order_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id UUID NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  product_id UUID REFERENCES products(id),
  product_name TEXT NOT NULL,
  product_image TEXT,
  price NUMERIC(10,2) NOT NULL,
  quantity INT NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 公开读取（配合订单权限）
CREATE POLICY "Read order items" ON order_items
  FOR SELECT USING (true);
```

### 2.7 admin_users（管理员表）

```sql
CREATE TABLE admin_users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL UNIQUE,
  role TEXT DEFAULT 'admin' CHECK (role IN ('admin', 'super_admin')),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 仅超级管理员可管理
CREATE POLICY "Super admin manage" ON admin_users
  FOR ALL USING (
    EXISTS (
      SELECT 1 FROM admin_users
      WHERE user_id = auth.uid() AND role = 'super_admin'
    )
  );
```

---

## 3. 存储过程 / 函数

### 3.1 生成订单号

```sql
CREATE OR REPLACE FUNCTION generate_order_number()
RETURNS TEXT AS $$
BEGIN
  RETURN 'FYG' || TO_CHAR(NOW(), 'YYYYMMDD') || LPAD(FLOOR(RANDOM() * 100000)::TEXT, 5, '0');
END;
$$ LANGUAGE plpgsql;
```

---

## 4. RLS 策略汇总

| 表 | SELECT | INSERT | UPDATE | DELETE |
|----|--------|--------|--------|--------|
| profiles | 所有者 | - | 所有者 | - |
| categories | 公开 | 管理员 | 管理员 | 管理员 |
| products | 公开(active) | 管理员 | 管理员 | 管理员 |
| orders | 所有者/管理员 | 所有者 | 管理员 | - |
| order_items | 配合 orders | 所有者 | - | - |
| admin_users | 超级管理员 | 超级管理员 | 超级管理员 | 超级管理员 |

---

## 5. 当前实现状态

| 功能 | 实现方式 | 说明 |
|------|----------|------|
| 用户认证 | localStorage | 模拟登录 |
| 产品数据 | 硬编码 | 未从 Supabase 读取 |
| 订单存储 | localStorage | 未持久化 |
| 分类管理 | Supabase | 后台可管理 |

---

## 6. 迁移计划

### 2.8 cart_items（购物车表）

```sql
CREATE TABLE cart_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES profiles(id) NOT NULL,
  product_slug TEXT NOT NULL,
  product_name TEXT NOT NULL,
  product_image TEXT,
  price NUMERIC(10,2) NOT NULL,
  quantity INT NOT NULL DEFAULT 1,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 用户只能操作自己的购物车
CREATE POLICY "Users manage own cart" ON cart_items
  FOR ALL USING (auth.uid() = user_id);
```

---

### Phase 1：数据迁移

1. 创建所有数据库表
2. 配置 RLS 策略
3. 迁移产品数据到 products 表

### Phase 2：功能对接

1. 修改前台代码，从 Supabase 读取产品
2. 接入 Supabase Auth
3. 订单数据持久化

### Phase 3：完善后台

1. 添加管理员创建功能
2. 完善权限控制

---

*文档结束*
